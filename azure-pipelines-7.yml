trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

# Run Cavisson Accessibility Scanner Extension
- task: CavissonAccessibilityScanner@1
  displayName: 'Run Accessibility Scan'
  inputs:
    targetUrl: 'https://www.cricbuzz.com/'
    failOnCritical: false
